[project]
name = "compute"
version = "0.1.0"
description = "OpenCapital compute sidecar"
requires-python = ">=3.14"
dependencies = [
  "polars>=1,<2",
  "pg8000>=1.30,<2",
  "sqlglot>=25,<27",
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
