"""Independent reference implementations of TWR, the forward-filled
returns grid, and rolling/expanding regression stats.

Test-only oracle: ``compute.metrics.*`` (polars) is cross-checked against
these straightforward versions in the parity tests.
"""
